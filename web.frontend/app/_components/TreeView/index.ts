/**
 * Entry công khai của module TreeView.
 * Import từ `@/app/_components/TreeView` để dùng component và kiểu TreeViewRef.
 */
export { default } from "./TreeView";
export type { TreeViewRef } from "./types";
